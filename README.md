# Cell Analysis Project

This is a simple Python script that analyzes a cell image.

It turns the image into grayscale, segments the cells, counts how many there are, and calculates their average size. It also shows a # Cell Analysis Project

This is a simple Python script that analyzes a cell image.

It turns the image into grayscale, segments the cells, counts how many there are, and calculates their average size. It also shows a few plots to help visualize the results.

# How to Run

Before running the script, make sure you have these Python packages installed:

- numpy
- matplotlib
- scikit-image

To run the script, use:

```bash
python cell_analyse.py